package enums;

public enum DownloaderConfigurations {
	username, password, max_concurrent_downloads, products_folder, intersectingPoint, dateStart, dateEnd, platformName, productType, cloudCoverPercentage, max_connections_per_time_window, time_window
}